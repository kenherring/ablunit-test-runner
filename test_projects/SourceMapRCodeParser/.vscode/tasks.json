{
	"version": "2.0.0",
	"options": {
		"env": {
			"PATH": "${env:PATH};${env:DLC}/ant/bin",
		}
	},
	"tasks": [
		{
			"label": "ant compile-and-test",
			"type": "shell",
			"command": "ant",
			"problemMatcher": [],
			"group": {
				"kind": "build",
				"isDefault": true
			}

		}
	]
}
